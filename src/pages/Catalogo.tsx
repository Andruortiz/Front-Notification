export default function Catalogo() {
    return (
        <div>
            <div className="page-header">
                <h1>Catálogo</h1>
                <p className="page-subtitle">Canales y proveedores registrados.</p>
            </div>
            <div className="state-message">
                Todavía no hay un endpoint para consultar el catálogo — queda pendiente del lado del
                backend (registro dinámico de canales/proveedores).
            </div>
        </div>
    );
}
